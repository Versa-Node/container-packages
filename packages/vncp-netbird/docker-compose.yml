services:
  vncp-netbird:
    image: ghcr.io/versa-node/vncp-netbird:latest
    container_name: vncp-netbird
    restart: unless-stopped
    env_file: ./.env
        networks: [versanode]

networks:
  versanode:
    external: true
    name: versanode
