# Autogenerated on host by cockpit tool
persistence true
persistence_location /mosquitto/data/

listener ${MQTT_PORT}
allow_anonymous ${ALLOW_ANONYMOUS}
password_file /mosquitto/config/passwords

% if ${WS_ENABLE} == "true":
listener ${WS_PORT}
protocol websockets
% endif
